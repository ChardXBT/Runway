# Runtime data

RunWay creates its SQLite database, media archive, browser profile, reports, raw records,
and diagnostic snapshots here. Runtime contents are deliberately ignored by Git. Never commit
the persistent browser profile or credentials.
